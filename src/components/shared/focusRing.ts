// Shared keyboard-focus ring — Reactor Violet outline offset from the near-black
// base, shown only on :focus-visible (keyboard/AT) so mouse clicks stay clean.
export const focusRing = 'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-purple focus-visible:ring-offset-2 focus-visible:ring-offset-bg-base rounded-sm'
